-- liquibase formatted sql

-- changeset aqua:1748090687562-3
CREATE TABLE hotel_room (id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, room_type BIGINT NOT NULL, hotel BIGINT, number VARCHAR(20) NOT NULL, floor SMALLINT NOT NULL, status VARCHAR(255) NOT NULL, price_per_night NUMERIC(10, 2) NOT NULL CHECK (price_per_night >= 1), created_at TIMESTAMP WITHOUT TIME ZONE NOT NULL, CONSTRAINT pk_hotelroom PRIMARY KEY (id));

-- changeset aqua:1748090687562-4
ALTER TABLE hotel_room ADD CONSTRAINT FK_HOTELROOM_ON_HOTEL FOREIGN KEY (hotel) REFERENCES hotel (id);

-- changeset aqua:1748090687562-5
ALTER TABLE hotel_room ADD CONSTRAINT FK_HOTELROOM_ON_ROOM_TYPE FOREIGN KEY (room_type) REFERENCES room_type (id);

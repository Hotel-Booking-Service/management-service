-- liquibase formatted sql

-- changeset kanaoki:1748348004323-1
CREATE TABLE hotel_photo (id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL, s3key VARCHAR(255) NOT NULL, hotel_id BIGINT, created_at TIMESTAMP WITHOUT TIME ZONE NOT NULL, CONSTRAINT pk_hotelphoto PRIMARY KEY (id));

-- changeset kanaoki:1748348004323-2
ALTER TABLE hotel_photo ADD CONSTRAINT UC_HOTELPHOTO_S3KEY UNIQUE (s3key);

-- changeset kanaoki:1748348004323-3
ALTER TABLE hotel_photo ADD CONSTRAINT FK_HOTELPHOTO_ON_HOTEL FOREIGN KEY (hotel_id) REFERENCES hotel (id);

